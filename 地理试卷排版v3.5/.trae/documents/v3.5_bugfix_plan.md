# 地理试卷排版 v3.5 修复计划

## Context

v3.5 系统采用"六步解耦流水线 + 代码/AI 双轨分流"架构，设计优秀但执行层存在严重缺陷。核心问题是**图片全链路断裂**（占位符正则不匹配导致全部图片丢失）和 **Schema 契约失效**。本计划按报告第 8.5 节"最终修复执行清单"的顺序，修复 P0+P1 级别的 8 个关键问题。

---

## 修复步骤

### 步骤 1: BUG-02 + BUG-03 — 修复图片丢失（最高优先级）

**文件**: `scripts/typeset_exam.py`

**修改 1a**: 第 61 行，修改正则，加捕获组并支持 ph_xxx / img_xxx / ph_anchor_xxx 三种格式

```python
# 修改前
PLACEHOLDER_TOKEN_PATTERN = re.compile(r'\{\{image:ph_\d{3}\}\}')

# 修改后
PLACEHOLDER_TOKEN_PATTERN = re.compile(r'\{\{image:(ph(?:_anchor)?_\d{3}|img_\d{3})\}\}')
```

**修改 1b**: 第 508 行，扩展 `_add_option_label_text` 中的占位符 ID 判断，支持 img_xxx 和 ph_anchor_xxx

```python
# 修改前
ph_file_path, exists = image_resolver.resolve(part) if part.startswith('ph_') else (None, False)

# 修改后
ph_file_path, exists = image_resolver.resolve(part) if (part.startswith('ph_') or part.startswith('img_')) else (None, False)
```

**修改 1c**: 第 647-669 行 `_format_rule_image`，将硬编码的 A/B/C/D 改为动态遍历

```python
# 修改后逻辑：按 sorted(options.keys()) 动态遍历，每行两个选项
letters = sorted(options.keys())
# 第一个段落：letters[0], letters[1]
# 第二个段落：letters[2], letters[3]
# 超过4个时继续添加段落
```

---

### 步骤 2: BUG-01 — 修复上下标字号被覆盖

**文件**: `scripts/typeset_exam.py`

**修改位置**: 第 237-251 行，`_add_format_tagged_text` 函数

问题：先设 `run.font.size = Pt(size_pt * 0.7)`，随后 `_set_run_font` 又传入 `size_pt` 将字号重置。

修复：对 sub/sup 标签的 run，`_set_run_font` 传入 `size_pt * 0.7` 而非 `size_pt`：

```python
# 修改前 (L247-251)
if seg_type == 'en':
    _set_run_font(run, cn_font=cn_font, en_font=en_font, size_pt=size_pt)
else:
    force = any(_is_cn_punct(c) for c in seg_text)
    _set_run_font(run, cn_font=cn_font, en_font=en_font, size_pt=size_pt, force_cn_all=force)

# 修改后
actual_size = size_pt * 0.7 if tag_type in ('sub', 'sup') else size_pt
if seg_type == 'en':
    _set_run_font(run, cn_font=cn_font, en_font=en_font, size_pt=actual_size)
else:
    force = any(_is_cn_punct(c) for c in seg_text)
    _set_run_font(run, cn_font=cn_font, en_font=en_font, size_pt=actual_size, force_cn_all=force)
```

同时删除 L242 和 L246 处重复的字号设置（由 `_set_run_font` 统一处理）。

---

### 步骤 3: BUG-04/05 — 修复 NameError 崩溃

**文件**: `scripts/typeset_exam.py`

**修改 3a (BUG-04)**: 第 847 行附近，`_format_materials_inner` 中，当材料仅含图片 token 时 `p` 变量未定义

在 for 循环前初始化 `p = None`，并在引用 `p` 的地方（L865 guide_sentence）增加 `if p is not None:` 判断：

```python
# L843 附近，在 for k, tp in enumerate(text_parts): 之前
p = None

# L864-865
if p is not None and guide_sentence:
    add_mixed_text(p, guide_sentence, cn_font='楷体')
elif guide_sentence:
    p = doc.add_paragraph()
    apply_style(p, 'Body Text')
    add_mixed_text(p, guide_sentence, cn_font='楷体')
```

**修改 3b (BUG-05)**: 第 905 行附近，`img_path` 在 `_ph_to_file` 为空时未定义

在 seg_type == 'image' 分支中，初始化 `img_path = None`，并在使用前检查：

```python
# L905 附近
elif seg_type == 'image':
    img_name = seg.get('name', '')
    img_path = None  # 初始化
    if img_name:
        ...
    if img_path and os.path.exists(img_path):
        add_centered_picture(doc, img_path, logger)
        quality['images_inserted'] += 1
    elif img_name:
        logger.warning(f'    图片未找到: {img_name}')
```

---

### 步骤 4: BUG-08 — 修复命令注入漏洞

**文件**: `scripts/map_images.py`

**修改位置**: 第 712-713 行

```python
# 修改前
validate_cmd = f'python "{resolve("scripts/validate_json.py")}" --schema "{schema_path}" --json "{output_path}" --quiet'
exit_code = os.system(validate_cmd)

# 修改后
import subprocess as sp
result = sp.run(
    [sys.executable, str(resolve("scripts/validate_json.py")),
     "--schema", schema_path, "--json", output_path, "--quiet"],
    capture_output=True
)
exit_code = result.returncode
```

---

### 步骤 5: BUG-06 — 修复表格内图片遗漏

**文件**: `scripts/extract_images.py`

**修改位置**: 第 77 行

```python
# 修改前
paragraphs = body.findall(qn('w:p'))

# 修改后（递归查找，包括表格内的段落）
paragraphs = body.findall(f'.//{qn("w:p")}')
```

---

### 步骤 6: SCHEMA-01/02 — 修复 Schema 契约违反

**方案**: 修改 Schema 兼容 `ph_anchor_001` 格式，并添加 `_source`/`_ai_reason` 字段定义。

**文件**: `schemas/exam_paper.schema.json`

**修改 6a**: 第 569-570 行，扩展 placeholder_id pattern

```json
"placeholder_id": {
  "type": "string",
  "description": "占位符唯一标识",
  "pattern": "^ph_(?:anchor_)?\\d{3}$"
}
```

**修改 6b**: 第 574-575 行，扩展 token pattern

```json
"token": {
  "type": "string",
  "description": "占位符标记文本",
  "pattern": "^\\{\\{image:ph_(?:anchor_)?\\d{3}\\}\\}$"
}
```

**修改 6c**: 第 597-604 行，在 properties 末尾添加 `_source` 和 `_ai_reason` 字段，并保持 `additionalProperties: false`

在 `reason` 和 `uncertain` 之间添加：

```json
"_source": {
  "type": "string",
  "description": "占位符来源标识（v3.5双轨分流）",
  "enum": ["inline", "anchor"]
},
"_ai_reason": {
  "type": "string",
  "description": "AI判断依据（anchor图专用，记录AI如何确定图片位置）"
}
```

---

### 步骤 7: ARCH-01 — 删除 3 个 v3.0 旧技能文件

删除以下文件：

| 文件 | 删除理由 |
|------|----------|
| `.trae/skills/tag_placeholders/SKILL.md` | v3.0 版，用 ph_xxx 格式，与 tag_placeholders_anchor 矛盾 |
| `.trae/skills/tag_images/SKILL.md` | v3.0 版，与 tag_images_anchor 矛盾 |
| `.trae/skills/geo-exam-formatting/SKILL.md` | 明确标注 "v3.0"，引用旧技能名，是 AI 误入口 |

---

### 步骤 8: BUG-09 — 修复 ASCII 标点宽度估算错误

**文件**: `scripts/typeset_exam.py`

**修改位置**: 第 268-277 行，`estimate_text_width_cm` 函数

问题：ASCII 标点（如逗号、句号、冒号）使用中文宽度，导致选项排版规则误判。

```python
# 修改后
def estimate_text_width_cm(text):
    width = 0.0
    for ch in text:
        if ch.isascii() and (ch.isdigit() or ch.isalpha()):
            width += _EN_CHAR_WIDTH_CM
        elif ch == "'":
            width += _EN_CHAR_WIDTH_CM
        elif ch.isascii():
            width += _EN_CHAR_WIDTH_CM  # ASCII 标点用英文宽度
        else:
            width += _CN_CHAR_WIDTH_CM
    return round(width, 2)
```

---

## 不修改的文件

- `scripts/batch_process.py` — 状态回写逻辑涉及 AI 步骤调度，改动范围大，归入中期改进
- `scripts/clean_docx.py` — 规则执行顺序问题影响较小，归入中期改进
- `scripts/sanitize_json.py` — JSON 解析启发式问题较复杂，归入中期改进
- `scripts/e2e_test.py` — 测试适配需完整端到端验证，归入中期改进

## 验证方式

完成步骤 1-7 后，可用以下方式验证核心修复：
1. 步骤 1 修复后：重新排版河南省洛阳市试卷，确认图片不再全部丢失
2. 步骤 2 修复后：检查含 `<sub>`/`<sup>` 文本的字号是否为 0.7 倍
3. 步骤 3 修复后：材料仅含图片 token 时不崩溃
4. 步骤 4 修复后：map_images.py 路径含空格时正常校验
5. 步骤 5 修复后：含表格内图片的试卷能提取出表格中的图片
6. 步骤 6 修复后：含 ph_anchor_001 占位符的 JSON 通过 Schema 校验
7. 步骤 7 修复后：.trae/skills/ 下仅剩 8 个技能目录
